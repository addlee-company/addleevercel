export const metadata = {
  title: 'Addlee - AI-Powered Creator & Hotel Matching',
  description: 'Addlee connects UGC creators with hotels using AI-powered matching for authentic content partnerships.',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
